{
  "name": "@demo/monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm run build",
    "test": "echo 'No tests configured'"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}